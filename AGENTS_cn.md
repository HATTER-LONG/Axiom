# Axiom Agent 开发指南

## 1. 核心原则

- 保持修改小而聚焦，每一步都应可独立验证。
- 优先小步迭代，不做大规模前期规划后一次性实现。
- Prompt 负责方向，确定性工具负责质量约束。
- 不得为了通过任务而跳过、关闭或降低质量检查。

## 2. 代码设计

- 优先采用 **薄接口 + 深模块**。
- Public API 应尽量少、稳定、清晰，并隐藏内部复杂度。
- 优先修改模块内部实现，而不是扩展 Public API。
- 除非模块本身就是适配层，否则不要将 Qt、OCC、Boost 等第三方实现类型扩散到模块边界之外。
- 保持单向依赖：低层模块不得反向依赖应用层、UI 层或测试代码。
- 优先使用 RAII、明确所有权和组合，避免不必要的复杂继承。
- 不为假想的未来需求提前设计复杂抽象。
- 不进行与当前任务无关的大范围重构。
- 避免创建职责模糊的 `utils`、`common`、`manager` 等万能模块。

## 3. 修改前

开始编码前确认：

1. 当前需求属于哪个模块。
2. 现有接口是否已经能够完成需求。
3. 是否真的需要修改 Public API。
4. 哪些现有行为不能改变。
5. 需要增加或修改哪些测试。

只阅读完成当前任务真正相关的代码，避免无目的扩大上下文。

## 4. 开发流程

每个小目标遵循：

```text
理解需求
  ↓
最小实现
  ↓
补充测试
  ↓
运行 fast
  ↓
修复失败
  ↓
进入下一步
```

每完成一个可验证的小目标：
uv run --quiet python tools/check.py fast
如果失败，根据 JSON 报告修复根因，并重新运行 fast。未通过前不得继续下一阶段。
较大任务完成后：
uv run --quiet python tools/check.py hardening
准备合并或完成整个开发任务后：
uv run --quiet python tools/check.py full
具体检查规则、阈值和工具参数以 tools/check.py 和 quality/ 配置为准，不在本文件重复维护。5. 测试原则

- 新增行为必须有对应测试。
- 修复 Bug 时应增加回归测试。
- 优先测试外部行为，而不是内部实现细节。
- Coverage 只是基础指标，不代表测试本身有效。
- 不得为了提高覆盖率编写没有有效断言的测试。

6. 架构约束
   架构依赖由 quality/architecture_rules.json 强制执行。
   基本原则：

- 低层不能依赖高层。
- Domain 不依赖 UI。
- Library 不依赖 App。
- Production 不依赖 Test。
  如果出现架构冲突，应通过调整职责、提取接口或反转依赖解决，不得绕过架构检查。

7. 多 Agent 协作
   复杂任务优先按阶段拆分：

```text
   Planner
   ↓
   Implementation
   ↓
   Cleanup / Review
   ↓
   Hardening
```

不同 Agent 应尽量使用独立、干净的上下文。
Agent 之间优先传递：

- 当前任务目标
- Acceptance Criteria
- Git Diff
- 测试结果
- Quality Gate 报告
- 尚未解决的问题
  不要把前一个 Agent 的完整长上下文直接传递给下一个 Agent。

8. 禁止事项

- 不得跳过 Quality Gate 后宣称任务完成。
- 不得为了通过检查降低质量标准。
- 不得无必要修改 Public API。
- 不得引入错误的模块依赖方向。
- 不得把第三方实现细节扩散到整个工程。
- 不得创建没有真实需求支撑的复杂抽象。
- 不得顺手进行大量无关重构。
- 不得通过关闭 warning、排除测试、忽略规则等方式隐藏真实问题。
- 不得在同一长期上下文中持续混合大量无关任务。

9. 完成标准
   任务完成必须满足：

- 需求已经正确实现。
- 修改范围保持最小。
- Public API 修改必要且合理。
- 模块依赖关系正确。
- 新行为有对应测试。
- fast 通过。
- 大任务 hardening 通过。
- 合并或最终交付前 full 通过。
  最终目标不是让 Agent “写出代码”，而是让 Agent 在清晰的架构边界和确定性质量门禁约束下，持续交付可信、可维护、可演进的代码。

# Axiom Agent 开发指南

## 1. 核心原则

- 保持修改小而聚焦，每一步都应可独立验证。
- 优先小步迭代，不做大规模前期规划后一次性实现。
- Prompt 负责方向，确定性工具负责质量约束。
- 不得为了通过任务而跳过、关闭或降低质量检查。

## 2. 代码设计

- 优先采用 **薄接口 + 深模块**。
- Public API 应尽量少、稳定、清晰，并隐藏内部复杂度。
- 优先修改模块内部实现，而不是扩展 Public API。
- 除非模块本身就是适配层，否则不要将 Qt、OCC、Boost 等第三方实现类型扩散到模块边界之外。
- 保持单向依赖：低层模块不得反向依赖应用层、UI 层或测试代码。
- 优先使用 RAII、明确所有权和组合，避免不必要的复杂继承。
- 不为假想的未来需求提前设计复杂抽象。
- 不进行与当前任务无关的大范围重构。
- 避免创建职责模糊的 `utils`、`common`、`manager` 等万能模块。

## 3. 修改前

开始编码前确认：

1. 当前需求属于哪个模块。
2. 现有接口是否已经能够完成需求。
3. 是否真的需要修改 Public API。
4. 哪些现有行为不能改变。
5. 需要增加或修改哪些测试。

只阅读完成当前任务真正相关的代码，避免无目的扩大上下文。

## 4. 开发流程

每个小目标遵循：

```text
理解需求
  ↓
最小实现
  ↓
补充测试
  ↓
运行 fast
  ↓
修复失败
  ↓
进入下一步
```

# Axiom Agent 开发指南

## 1. 核心原则

- 保持修改小而聚焦，每一步都应可独立验证。
- 优先小步迭代，不做大规模前期规划后一次性实现。
- Prompt 负责方向，确定性工具负责质量约束。
- 不得为了通过任务而跳过、关闭或降低质量检查。

## 2. 代码设计

- 优先采用 **薄接口 + 深模块**。
- Public API 应尽量少、稳定、清晰，并隐藏内部复杂度。
- 优先修改模块内部实现，而不是扩展 Public API。
- 除非模块本身就是适配层，否则不要将 Qt、OCC、Boost 等第三方实现类型扩散到模块边界之外。
- 保持单向依赖：低层模块不得反向依赖应用层、UI 层或测试代码。
- 优先使用 RAII、明确所有权和组合，避免不必要的复杂继承。
- 不为假想的未来需求提前设计复杂抽象。
- 不进行与当前任务无关的大范围重构。
- 避免创建职责模糊的 `utils`、`common`、`manager` 等万能模块。

## 3. 修改前

开始编码前确认：

1. 当前需求属于哪个模块。
2. 现有接口是否已经能够完成需求。
3. 是否真的需要修改 Public API。
4. 哪些现有行为不能改变。
5. 需要增加或修改哪些测试。

只阅读完成当前任务真正相关的代码，避免无目的扩大上下文。

## 4. 开发流程

每个小目标遵循：

```text
理解需求
  ↓
最小实现
  ↓
补充测试
  ↓
运行 fast
  ↓
修复失败
  ↓
进入下一步
```

每完成一个可验证的小目标：
uv run --quiet python tools/check.py fast
如果失败，根据 JSON 报告修复根因，并重新运行 fast。未通过前不得继续下一阶段。
较大任务完成后：
uv run --quiet python tools/check.py hardening
准备合并或完成整个开发任务后：
uv run --quiet python tools/check.py full
具体检查规则、阈值和工具参数以 tools/check.py 和 quality/ 配置为准，不在本文件重复维护。5. 测试原则

- 新增行为必须有对应测试。
- 修复 Bug 时应增加回归测试。
- 优先测试外部行为，而不是内部实现细节。
- Coverage 只是基础指标，不代表测试本身有效。
- 不得为了提高覆盖率编写没有有效断言的测试。

6. 架构约束
   架构依赖由 quality/architecture_rules.json 强制执行。
   基本原则：

- 低层不能依赖高层。
- Domain 不依赖 UI。
- Library 不依赖 App。
- Production 不依赖 Test。
  如果出现架构冲突，应通过调整职责、提取接口或反转依赖解决，不得绕过架构检查。

7. 多 Agent 协作
   复杂任务优先按阶段拆分：
   Planner
   ↓
   Implementation
   ↓
   Cleanup / Review
   ↓
   Hardening
   不同 Agent 应尽量使用独立、干净的上下文。
   Agent 之间优先传递：

- 当前任务目标
- Acceptance Criteria
- Git Diff
- 测试结果
- Quality Gate 报告
- 尚未解决的问题
  不要把前一个 Agent 的完整长上下文直接传递给下一个 Agent。

8. 禁止事项

- 不得跳过 Quality Gate 后宣称任务完成。
- 不得为了通过检查降低质量标准。
- 不得无必要修改 Public API。
- 不得引入错误的模块依赖方向。
- 不得把第三方实现细节扩散到整个工程。
- 不得创建没有真实需求支撑的复杂抽象。
- 不得顺手进行大量无关重构。
- 不得通过关闭 warning、排除测试、忽略规则等方式隐藏真实问题。
- 不得在同一长期上下文中持续混合大量无关任务。

9. 完成标准
   任务完成必须满足：

- 需求已经正确实现。
- 修改范围保持最小。
- Public API 修改必要且合理。
- 模块依赖关系正确。
- 新行为有对应测试。
- fast 通过。
- 大任务 hardening 通过。
- 合并或最终交付前 full 通过。
  最终目标不是让 Agent “写出代码”，而是让 Agent 在清晰的架构边界和确定性质量门禁约束下，持续交付可信、可维护、可演进的代码。

每完成一个可验证的小目标：
uv run --quiet python tools/check.py fast
如果失败，根据 JSON 报告修复根因，并重新运行 fast。未通过前不得继续下一阶段。
较大任务完成后：
uv run --quiet python tools/check.py hardening
准备合并或完成整个开发任务后：
uv run --quiet python tools/check.py full
具体检查规则、阈值和工具参数以 tools/check.py 和 quality/ 配置为准，不在本文件重复维护。5. 测试原则

- 新增行为必须有对应测试。
- 修复 Bug 时应增加回归测试。
- 优先测试外部行为，而不是内部实现细节。
- Coverage 只是基础指标，不代表测试本身有效。
- 不得为了提高覆盖率编写没有有效断言的测试。

6. 架构约束
   架构依赖由 quality/architecture_rules.json 强制执行。
   基本原则：

- 低层不能依赖高层。
- Domain 不依赖 UI。
- Library 不依赖 App。
- Production 不依赖 Test。
  如果出现架构冲突，应通过调整职责、提取接口或反转依赖解决，不得绕过架构检查。

7. 多 Agent 协作
   复杂任务优先按阶段拆分：
   Planner
   ↓
   Implementation
   ↓
   Cleanup / Review
   ↓
   Hardening
   不同 Agent 应尽量使用独立、干净的上下文。
   Agent 之间优先传递：

- 当前任务目标
- Acceptance Criteria
- Git Diff
- 测试结果
- Quality Gate 报告
- 尚未解决的问题
  不要把前一个 Agent 的完整长上下文直接传递给下一个 Agent。

8. 禁止事项

- 不得跳过 Quality Gate 后宣称任务完成。
- 不得为了通过检查降低质量标准。
- 不得无必要修改 Public API。
- 不得引入错误的模块依赖方向。
- 不得把第三方实现细节扩散到整个工程。
- 不得创建没有真实需求支撑的复杂抽象。
- 不得顺手进行大量无关重构。
- 不得通过关闭 warning、排除测试、忽略规则等方式隐藏真实问题。
- 不得在同一长期上下文中持续混合大量无关任务。

9. 完成标准
   任务完成必须满足：

- 需求已经正确实现。
- 修改范围保持最小。
- Public API 修改必要且合理。
- 模块依赖关系正确。
- 新行为有对应测试。
- fast 通过。
- 大任务 hardening 通过。
- 合并或最终交付前 full 通过。
  最终目标不是让 Agent “写出代码”，而是让 Agent 在清晰的架构边界和确定性质量门禁约束下，持续交付可信、可维护、可演进的代码。
