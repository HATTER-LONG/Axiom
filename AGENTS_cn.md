# Axiom Agent 开发指南

## 1. 核心原则

- 保持修改小而聚焦；每一步都应可独立验证。
- 优先增量开发，而不是先做大规模规划、最后一次性实现。
- Prompt 提供方向，确定性工具保障质量。
- 不得为了让任务通过而跳过、禁用、绕过或降低质量检查。

## 2. 代码设计

- 优先采用**薄接口、深模块**。
- Public API 应保持最小、稳定、清晰，并隐藏内部复杂性。
- 优先修改模块内部实现，而不是扩展 Public API。
- 除非模块明确是适配层，否则不要跨模块边界暴露 Qt、OCC、Boost 等第三方实现类型。
- 保持单向依赖：
  - 低层模块不得依赖应用层模块。
  - 低层模块不得依赖 UI 代码。
  - 生产代码不得依赖测试代码。
- 优先使用 RAII、明确所有权和组合，避免不必要的复杂继承。
- 不为假设性的未来需求引入复杂抽象。
- 不做与当前任务无关的大范围重构。
- 避免创建 `utils`、`common`、`manager` 等职责模糊的万能模块。

## 3. 修改前

编码前确定：

1. 哪个模块拥有所请求的行为。
2. 现有接口是否已能满足需求。
3. 修改 Public API 是否确有必要。
4. 哪些既有行为必须保持不变。
5. 需要新增或更新哪些测试。

只阅读与当前任务真正相关的代码；不要无明确理由地扩大工作上下文。

## 4. 开发流程

每个小开发目标应遵循：

```text
理解需求
        ↓
最小实现
        ↓
新增或更新测试
        ↓
运行 fast
        ↓
修复失败
        ↓
进入下一步
```

每完成一个可独立验证的目标后，运行：

```bash
uv run --quiet python tools/check.py fast
```

若失败：

- 检查 JSON 报告。
- 找出并修复根因。
- 再次运行 `fast`。
- 在通过前不得进入下一开发阶段。

对于较大任务，运行：

```bash
uv run --quiet python tools/check.py hardening
```

合并或宣称整体任务完成前，运行：

```bash
uv run --quiet python tools/check.py full
```

检查、阈值和工具参数的权威定义在 `tools/check.py` 及 `quality/` 下的配置中。
日常功能开发时，应将质量运行器视为黑盒契约：运行所需门禁，再根据其 JSON
报告决定下一步修复。不得为了了解门禁实现或预测是否会通过，而主动阅读
`tools/check.py`、`tools/checks/`、`tools/check_test/` 或 `quality/`。

仅在以下情况确有必要时才阅读实现：（1）要修改质量系统或其策略；（2）门禁
失败，但 JSON 报告以及相关编译器/测试输出不足以指导修复；（3）已有具体证据
表明检查本身存在缺陷。此时只检查诊断所需的文件。不要在本文件中重复质量系统
细节。

### IWYU Finding

将 IWYU finding 视为诊断信息，而不是自动修改指令。先判断报告的 include 是否由文件的 Public API、直接使用或调用点真正需要；若 finding 有效，优先进行最小且真实的 include 修复。

优先移除已证实冗余的 include。只有在确认文件直接使用的符号没有被有意的直接 include 提供其公开声明时，才新增 include。当符号已由更具体的公开头文件提供时，不得仅因 IWYU 建议就添加更宽泛的标准库头文件。

若 finding 已被确认是 IWYU 误报，可以在 `quality/iwyu.imp` 新增或更新一个范围严格受限、由仓库维护的 mapping。首次引入时必须将该 mapping 接入 IWYU 调用，记录它所表达的具体头文件关系或工具限制，并且只抑制这一关系。例如，应将错误归属的标准库符号映射至其具体的公开头文件，而不是添加无关的聚合头文件。不得用宽泛 mapping、全局抑制、跳过检查或移除诊断来隐藏真实 include 依赖。

无论修改 include 还是 mapping，都必须运行适用的质量门禁，并确认受影响的翻译单元仍能编译，且 finding 被解决而没有引入新的架构依赖。

## 5. 测试原则

- 每项新增行为必须有对应测试。
- Bug 修复应包含回归测试。
- 优先测试外部可观察行为，而不是内部实现细节。
- 覆盖率只是基础指标，不能证明测试有效。
- 不得为了提高覆盖率添加没有意义或断言薄弱的测试。

## 6. 架构约束

架构依赖由以下文件强制执行：

```text
quality/architecture_rules.json
```

基本规则：

- 低层模块不得依赖高层模块。
- Domain 代码不得依赖 UI 代码。
- Library 不得依赖应用。
- 生产代码不得依赖测试代码。

出现架构违规时，应通过修正职责、抽取接口或依赖倒置来解决。

绝不可绕过或禁用架构检查。

## 7. 多 Agent 协作

不同 Agent 应尽量使用独立且干净的上下文。

Agent 交接时，只传递继续工作所需的信息：

- 当前任务目标
- 验收标准
- Git Diff
- 测试结果
- Quality Gate 报告
- 未解决问题

不要将一个 Agent 的完整长上下文直接交给下一个 Agent。

仓库状态、代码改动、测试和确定性报告是主要共享事实来源。

## 8. 禁止事项

不得：

- 未通过所需 Quality Gate 就宣称任务完成。
- 为了通过检查而降低质量标准。
- 未经合理需求修改 Public API。
- 引入错误的模块依赖方向。
- 不必要地将第三方实现细节泄漏到模块边界之外。
- 没有具体需求就创建复杂抽象。
- 在聚焦任务中进行大量无关重构。
- 通过禁用 warning、排除测试、忽略规则或削弱检查来隐藏真实问题。
- 在同一个长期 Agent 上下文中持续混合大量无关任务。

## 9. 完成定义

任务只有在满足以下条件时才算完成：

- 所请求行为已正确实现。
- 修改范围最小且聚焦。
- Public API 变更确有必要且合理。
- 模块依赖方向仍正确。
- 新行为有对应测试。
- `fast` 通过。
- 较大任务的 `hardening` 通过。
- 合并或最终交付前 `full` 通过。

目标不只是让 Agent **写代码**。

目标是在明确架构边界和确定性质量门禁下，持续交付**可靠、可维护且可演进的软件**。
