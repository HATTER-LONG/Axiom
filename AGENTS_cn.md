# Axiom Agent 开发指南

## 1. 核心原则

- 保持修改小而聚焦；每一步都应可独立验证。
- 优先增量开发，而不是先做大规模规划、最后一次性实现。
- Prompt 提供方向，确定性工具保障质量。
- 不得为了让任务通过而跳过、禁用、绕过或降低质量检查。

## 2. 代码设计

- 优先采用**薄接口、深模块**。
- Public API 应保持最小、稳定、清晰，并隐藏内部复杂性。
- 优先修改模块内部实现，而不是扩展 Public API。
- 除非模块明确是适配层，否则不要跨模块边界暴露 Qt、OCC、Boost 等第三方实现类型。
- 保持单向依赖：
  - 低层模块不得依赖应用层模块。
  - 低层模块不得依赖 UI 代码。
  - 生产代码不得依赖测试代码。
- 优先使用 RAII、明确所有权和组合，避免不必要的复杂继承。
- 不为假设性的未来需求引入复杂抽象。
- 不做与当前任务无关的大范围重构。
- 避免创建 `utils`、`common`、`manager` 等职责模糊的万能模块。

## 3. 修改前

编码前确定：

1. 哪个模块拥有所请求的行为。
2. 现有接口是否已能满足需求。
3. 修改 Public API 是否确有必要。
4. 哪些既有行为必须保持不变。
5. 需要新增或更新哪些测试。

只阅读与当前任务真正相关的代码；不要无明确理由地扩大工作上下文。

## 4. 开发流程

每个小开发目标应遵循：

```text
理解需求
        ↓
最小实现
        ↓
新增或更新测试
        ↓
运行 fast
        ↓
修复失败
        ↓
进入下一步
```

每完成一个可独立验证的目标后，运行：

```bash
uv run --quiet checkflow fast --report build-quality/reports/fast.json
```

若失败：

- 检查 JSON 报告。
- 找出并修复根因。
- 再次运行 `fast`。
- 在通过前不得进入下一开发阶段。

对于较大任务，运行：

```bash
uv run --quiet checkflow hardening --report build-quality/reports/hardening.json
```

合并或宣称整体任务完成前，运行：

```bash
uv run --quiet checkflow full --report build-quality/reports/full.json
```

流程的权威定义在 `checkflow.json`；Axiom 专属 Tool 位于 `.checkflow/tools/`，策略仍位于 `quality/`。

不要在本文件中重复这些细节。

### 文档注释

- Public interface 以及不直观的内部类型、函数、不变量、所有权规则、状态转换和算法，都应使用兼容 Doxygen 的注释；文档注释不应只覆盖 API 方法摘要。
- 注释应描述用途和外部可观察行为。对于可调用接口，用 `@param` 描述每个参数，用 `@return` 描述非 `void` 返回值，用 `@throws` 描述所有可能向外传播的异常；必要时补充 `@pre`、`@post`、`@note` 或 `@warning`。
- 注释必须与行为同步，避免只是逐句复述代码。

```cpp
/**
 * @brief 加载并校验项目配置。
 *
 * 返回的配置拥有所有解析结果；输入流销毁后仍可安全持有。
 *
 * @param input 指向配置文档开头的 UTF-8 输入流。
 * @param source_name 校验失败时用于报告错误的来源名称。
 * @return 已应用默认值的有效配置。
 * @throws ParseError 文档格式错误时抛出。
 * @throws ValidationError 必填值缺失或无效时抛出。
 */
Configuration loadConfiguration(std::istream& input, std::string_view source_name);
```

### clang-tidy 抑制注释

不得仅为了让 clang-tidy 通过而使用 `NOLINT`、`NOLINTNEXTLINE`、`NOLINTBEGIN`、编译器 pragma 或等效注释。必须先确认代码本身正确、诊断属于误报或确实不适用于该处，并确认正常修改反而会降低正确性或清晰度。确有必要时，抑制必须指明具体 check、限制在最小作用域，并写明可验证的理由；禁止使用宽泛抑制。

## 5. 测试原则

- 每项新增行为必须有对应测试。
- Bug 修复应包含回归测试。
- 优先测试外部可观察行为，而不是内部实现细节。
- 覆盖率只是基础指标，不能证明测试有效。
- 不得为了提高覆盖率添加没有意义或断言薄弱的测试。

### GTEST 最佳实践

所有单元与集成测试均使用 GoogleTest 框架（通过 CPM 引入）。

- 测试用例使用 `TEST(SuiteName, TestName)` 命名，名称应描述所验证的单一可观察行为。
- 选择与比较语义最匹配的断言宏（例如 C 字符串比较用 `EXPECT_STREQ`，而非对指针用 `EXPECT_EQ`）。
- 仅当后续代码依赖前置条件（例如随后解引用某指针）时才用 `ASSERT_*` 保护；其余场景使用 `EXPECT_*`，以便单次运行暴露多个失败。
- 保持测试确定性：避免依赖系统时间、随机数与环境变量；需要时可注入或用夹具固化可变因素。
- 使用 `TEST_F` 夹具共享初始化，并通过 RAII（构造函数、`SetUp`/`TearDown`）管理资源。
- 断言有意义、可外部观察的结果；绝不只为了提高覆盖率编写空泛或琐碎的断言。

## 6. 架构约束

架构依赖由以下文件强制执行：

```text
quality/architecture_rules.json
```

基本规则：

- 低层模块不得依赖高层模块。
- Domain 代码不得依赖 UI 代码。
- Library 不得依赖应用。
- 生产代码不得依赖测试代码。

出现架构违规时，应通过修正职责、抽取接口或依赖倒置来解决。

绝不可绕过或禁用架构检查。

## 7. 多 Agent 协作

不同 Agent 应尽量使用独立且干净的上下文。

### Git 管理的任务边界

- 开始任务前先检查 `git status --short`，不得接管无关改动。
- 每个 Agent 同时只拥有一个边界明确的任务。返回、切换任务或交接前，必须提交本任务的全部改动；任何任务边界都不得遗留未暂存或已暂存未提交的内容。
- 每个 Agent 完成修改后、提交前，必须最终运行一次 `fast`。若失败，必须修复根因并重新运行；不得把失败任务作为完成状态提交。
- 每个任务提交都必须使用简洁且可读的 log，说明任务产出；修复提交必须说明修复的具体问题，不能只写“fix”。
- 不得把无关工作混入同一提交。提交历史是长期交接与 review 记录。

### 基于提交的 Review 与跟进

- 主 Agent 只 review 已提交的工作，绝不 review Agent 的未提交 diff。必须用 `git log` 检查任务提交，并使用 `git diff <base>..HEAD` 或 `git show` review 已提交范围。
- 在交接中记录被 review 的提交范围；未提交的工作不能产生 review 结论。
- reviewer 返回问题后，应保留并复用同一个 reviewer Agent。该 reviewer 必须检查修复提交，并继续 review 任务后续的新提交；比较对象应是上一次已 review 的提交范围，而不是未提交工作区。

Agent 交接时，只传递继续工作所需的信息：

- 当前任务目标
- 验收标准
- 已提交范围与简要 commit log
- 测试结果
- Quality Gate 报告
- 未解决问题

不要将一个 Agent 的完整长上下文直接交给下一个 Agent。

已提交的仓库历史、测试和确定性报告是主要共享事实来源。

## 8. 禁止事项

不得：

- 未通过所需 Quality Gate 就宣称任务完成。
- 为了通过检查而降低质量标准。
- 未经合理需求修改 Public API。
- 引入错误的模块依赖方向。
- 不必要地将第三方实现细节泄漏到模块边界之外。
- 没有具体需求就创建复杂抽象。
- 在聚焦任务中进行大量无关重构。
- 通过禁用 warning、排除测试、忽略规则或削弱检查来隐藏真实问题。
- 在同一个长期 Agent 上下文中持续混合大量无关任务。

## 9. 完成定义

任务只有在满足以下条件时才算完成：

- 所请求行为已正确实现。
- 修改范围最小且聚焦。
- Public API 变更确有必要且合理。
- 模块依赖方向仍正确。
- 新行为有对应测试。
- `fast` 通过。
- 较大任务的 `hardening` 通过。
- 合并或最终交付前 `full` 通过。

目标不只是让 Agent **写代码**。

目标是在明确架构边界和确定性质量门禁下，持续交付**可靠、可维护且可演进的软件**。
